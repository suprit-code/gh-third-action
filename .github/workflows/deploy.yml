name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGODB_DB_NAME: gha-demo
  PORT : 8080
jobs:
  test:
    environment: testing
    runs-on: ubuntu-latest
    env :
      MONGODB_CLUSTER_ADDRESS : cluster0.qq9qyqn.mongodb.net
      MONGODB_USERNAME : supritnaik100_db_user
      MONGODB_PASSWORD : OSYV8NFkG77sxmsc 
    steps:
      - name: Get Code
        uses: actions/checkout@v7
      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Run tests
        run: npm test
      - name: Output information
        run: |
          echo "ENVIRONMENT VARIABLES : ${{ env }}"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "ENVIRONMENT VARIABLES: ${{ env }}"